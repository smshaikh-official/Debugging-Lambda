{
  "name": "debugging-lambda",
  "version": "1.0.0",
  "description": "Source code demonstrating how to debug AWS lambda functions on development/local machine",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:debug": "node ./bin/build.js"
  },
  "author": "Shoaib Shaikh",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.714.0",
    "@types/node": "^22.10.2",
    "lambda-api": "^1.1.0"
  }
}
